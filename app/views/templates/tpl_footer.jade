.footer-bg
    .container
        .row
            .col-xs-12
                ul.list-inline
                    each name, code in languages
                        li
                            - var file = ''
                            if fileExt
                                - file = '/index.html'
                            a.reload(href='../' + code + file)= name