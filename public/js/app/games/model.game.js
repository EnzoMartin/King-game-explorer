/**
 * @name Game Model
 * @module Games
 * @memberOf Model
 */
define([
    'BB',
    'backbone'
],function(BB,Backbone){
    return BB.model_definitions.game = Backbone.Model.extend({
        defaults:{

        }
    });
});